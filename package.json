{
  "name": "conversationalist",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint --project .",
    "test": "jest",
    "docs:generate": "yarn typedoc --theme ./typedoc-jazzy-theme --excludeNotExported --excludePrivate --excludeExternals --exclude TypedEvent --out docs src"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.6.2",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "tslint-eslint-rules": "^5.4.0",
    "typedoc": "^0.15.0",
    "typescript": "3.7.0-beta"
  },
  "resolutions": {
    "typescript": "3.7.0-beta"
  }
}
